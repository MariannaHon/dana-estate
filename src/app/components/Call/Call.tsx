import React from 'react';
import css from './Call.module.css';

const Call = () => {
    return (
        <div>
            <button className={css.btn}>Order a call</button>
        </div>
    )
}

export default Call
